@using Connect.Razor.Blade;
@{
    var links = PageData["Links"];
    var Lib = PageData["Lib"];
}

<table @Edit.TagToolbar(Content) class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>@App.Resources.TableHlKey</th>
            <th>@App.Resources.TableHlGroup</th>
            <th>@App.Resources.TableHlTitle</th>
            <th>@App.Resources.TableHlTarget</th>
            <th class="text-center" title="Enabled or Retired/Disabled">✔️</th>
            <th class="text-center" title="Show to user">👁️</th>
            <th class="text-center">@App.Resources.TableHlQR</th>
            <th class="text-center">@App.Resources.TableHlTests</th>
        </tr>       
    </thead>
    <tbody>
    @foreach(var link in links) {
        var full = App.Settings.Protocol + App.Settings.ShortDomain + "/" + link.Key;
        
        // only for pro-feature
        var proDiffUrl = App.Settings.EnableQrUseTracking
            ? App.Settings.Protocol + App.Settings.ShortDomain + "/" + Lib.PreprocessUrl(link.Key)
            : full;

        var qr = Lib.QrLink(full);
        <tr>
            <td>
                <a href="javascript:;" onclick="$2sxc(this).manage.run('edit', {entityId: @link.EntityId})">
                    @link.Key
                </a>
            </td>
            <td>
                @{
                    var cat = ((IEnumerable<dynamic>)link.Group).FirstOrDefault();
                    var catTitle = cat != null ? cat.Title : "";
                }
                @catTitle
            </td>
            <td>@link.OLDTitle</td>
            <td>
                <a href="@link.Link" target='_blank'>
                    @link.Link
                </a>
            </td>
            <td class="text-center">@(link.Retired == true ? "🚫" : "✔️")</td>
            <td class="text-center">@(link.Show == true ? "👁": "")</td>
            <td class="text-center">
                <a href="@qr" target="_blank">
                    png
                </a>
                @if(App.Settings.EnablePro) {
                    <br>
                    <a href="@Lib.QrSvgLink(full)" target="_blank">
                        svg
                    </a>
                    <a href="@Lib.QrEpsLink(full)" target="_blank">
                        eps
                    </a>
                }
            </td>
            <td class="text-center">
                <ul class="list-inline" >
                    <li class="list-inline-item">
                        <a class="is-not-underlined" href="@full.Replace("https:","http:")" target="_blank" title="@App.Settings.ShortDomain/@link.Key">🔗</a>
                        @if(App.Settings.EnableQrUseTracking) 
                        {
                            <a class="is-not-underlined" href="@proDiffUrl.Replace("https:","http:")" target="_blank" title="@App.Settings.ShortDomain/@Lib.PreprocessUrl(link.Key)">⚡</a>
                        }
                    </li> 
                    <li class="list-inline-item">
                        <a class="is-not-underlined" href="@Lib.TestLink(link.Key, false)" target="_blank">#️⃣</a>
                        @if(App.Settings.EnableQrUseTracking) 
                        {
                            <a class="is-not-underlined" href="@Lib.TestLink(link.Key, true)" target="_blank">⚡</a>
                        }
                    </li>
                </ul>
            </td>
        </tr>
    }
    </tbody>
</table>
           